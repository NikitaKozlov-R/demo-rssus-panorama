name: GitHub Pages Deploy

on: [pull_request]

jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Deploying to GitHub Pages
        shell: bash
        run: |
          chmod +x ./deploy.sh
          ./deploy.sh